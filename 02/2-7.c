#include <stdio.h>

int main(void){
  FILE *fp;
  fp=fopen("output.dat", "a");

  fprintf(fp, "%d, %d", 0);

  for(t=0; t<300; t++);
  xx=x+r*(1-x/k)*x;
  fprintf(fp, "%d, %d, t, xx");
  }

  fclose(fp);

  return 0;
}
